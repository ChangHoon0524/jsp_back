<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="file">
   <!-- 파일 추가 -->
   <insert id="insert" parameterType="FileDTO">
      INSERT INTO tbl_file(file_system_name, file_original_name, board_number)
      VALUES (#{fileSystemName}, #{fileOriginalName}, #{boardNumber})
   </insert>
   
   <!-- 파일 조회 -->
   <select id="select" parameterType="int" resultType="FileDTO">
      SELECT file_system_name, file_original_name, board_number
      FROM tbl_file
      WHERE board_number = #{boardNumber}
   </select>
   
   <!-- 파일 삭제 -->
   <delete id="delete" parameterType="int">
      DELETE FROM tbl_file
      WHERE board_number = #{boardNumber}
   </delete>
</mapper>
